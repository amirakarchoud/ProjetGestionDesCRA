import { Absence } from "./Absence";
import { Activity } from "./Activity";
import { Collab } from "./Collab";
import { Etat } from "./etat.enum";
import { Holiday } from "./Holiday";
export declare class CRA {
    private _id;
    private _holidays;
    private _absences;
    private _activites;
    private _month;
    private _year;
    private _collab;
    private _date;
    private _etat;
    constructor(id: number, month: number, year: number, collab: Collab, date: Date, etat: Etat);
    get etat(): Etat;
    set etat(etat: Etat);
    checkActivityOrAbsenceExists(date: Date, matin: boolean): boolean;
    calculateBusinessDays(year: number, month: number): number;
    get activities(): Activity[];
    get absences(): Absence[];
    set absences(abs: Absence[]);
    set activities(act: Activity[]);
    set holidays(holidays: Holiday[]);
    addActivity(activity: Activity): void;
    addAbsence(absence: Absence): void;
    calculateEmptyDays(): number;
    verifyTotalDays(): boolean;
    get id(): number;
    get holidays(): Holiday[];
    get month(): number;
    get year(): number;
    get date(): Date;
    get collab(): Collab;
    verifyDateNotInCRA(date: Date, periode: boolean): boolean;
    formatDate(date: Date): string;
    deleteAbsence(date: Date, matin: boolean): void;
    deleteActivity(date: Date, matin: boolean): void;
    getAvailableDatesOfCra(): Date[];
    isWeekend(date: Date): boolean;
    checkDateIsHoliday(date: Date): Boolean;
    checkDayIsFull(date: Date): Boolean;
    isSameDate(date1: Date, date2: Date): boolean;
    SubmitCra(): Boolean;
}
